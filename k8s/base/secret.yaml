# Note: This file contains REAL local secrets for the GitOps demo.
# IMPORTANT: Committing real secrets to Git is NOT a real production practice.
# This is a deliberate, explicitly-flagged simplification being made for this project's local-demo scope only.
# In a real production setup, you should use Sealed Secrets, External Secrets Operator, or a vault-based solution instead.
apiVersion: v1
kind: Secret
metadata:
  name: subtrack-secret
  namespace: subtrack
  labels:
    app: subtrack
type: Opaque
stringData:
  MONGO_URI: "mongodb://subtrack_admin:change_me_locally@mongo:27017/subtrack?authSource=admin"
  JWT_SECRET: "super_secret_jwt_key_123_local"
  MONGO_ROOT_USERNAME: "subtrack_admin"
  MONGO_ROOT_PASSWORD: "change_me_locally"
