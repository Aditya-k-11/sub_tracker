apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: subtrack
  labels:
    app: subtrack
    component: mongo
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: subtrack
    component: mongo
