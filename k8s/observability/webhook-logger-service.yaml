apiVersion: v1
kind: Service
metadata:
  name: alertmanager-webhook-logger
  namespace: observability
spec:
  type: ClusterIP
  selector:
    app: alertmanager-webhook-logger
  ports:
    - port: 8080
      targetPort: 8080
      name: web
