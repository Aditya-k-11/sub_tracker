# Note: This is a placeholder file for the SubTrack Secret.
# For local testing, we are using a 'secret.local.yaml' file that is NOT committed to Git.
#
# IMPORTANT: In a production GitOps environment (like ArgoCD in Phase 11),
# you MUST use a proper secrets management solution (e.g. Sealed Secrets,
# External Secrets Operator) instead of plain text, or document that this 
# placeholder must be manually replaced post-clone.
# This plain placeholder approach is a known simplification for this project's scope.
apiVersion: v1
kind: Secret
metadata:
  name: subtrack-secret
  namespace: subtrack
  labels:
    app: subtrack
type: Opaque
stringData:
  MONGO_URI: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
  MONGO_ROOT_USERNAME: "REPLACE_ME"
  MONGO_ROOT_PASSWORD: "REPLACE_ME"
