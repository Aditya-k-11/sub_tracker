server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Fallback to index.html for React Router client-side routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Don't cache index.html so updates are visible immediately
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires 0;
    }

    # Proxy API requests to the backend container
    # NOTE: "backend" relies on Docker Compose's internal networking (Phase 9.3)
    # where the backend service will resolve automatically.
    location /api {
        proxy_pass http://backend:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
