# This is the committed version of the secret containing placeholder values.
# The real values are managed locally in grafana-secret.local.yaml which is gitignored,
# following the deliberate security tradeoff established in phase 10.2.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: observability
type: Opaque
stringData:
  admin-password: "admin"
