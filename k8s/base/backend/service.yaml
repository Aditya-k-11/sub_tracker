apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: subtrack
  labels:
    app: subtrack
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
  selector:
    app: subtrack
    component: backend
